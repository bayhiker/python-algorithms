class Solution:
    def findLucky(self, arr: list[int]) -> int:
        return self.find_lucky(arr)

    def find_lucky(self, arr: list[int]) -> int:
        # Use hash, or collections.Counter in python
        # Look through freq, record lucky numbers and keep track of the largest
        pass
