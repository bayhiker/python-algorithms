class Solution:
    def getKth(self, lo: int, hi: int, k: int) -> int:
        # Use dict to cache power value of number already calculated
        # When calculating power value of a new number, check cache along the way
        # and add that number directly, no need to re-calc
        pass
