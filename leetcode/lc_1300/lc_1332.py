class Solution:
    def removePalindromeSub(self, s: str) -> int:
        return self.remove_palindrome_sub(s)

    def remove_palindrome_sub(self, s: str) -> int:
        # Note that subsequence is different from substring,
        # a subsequence doesn't have to be consecutive.
        # Use two pointers to find max sub-sequence,
        # repeating until the string is empty
        pass
