class Solution:
    def countHomogenous(self, s: str) -> int:
        # Segment strings into substrings with repeating letters only, this is O(n)
        # For each such substring of length k, there are  k(K+1)/2 homogenous substrings
        # add all of them up.
        pass
