class Solution:
    def findLUSlength(self, a: str, b: str) -> int:
        return self.find_lus_length(a, b)

    def find_lus_length(self, a: str, b: str) -> int:
        # Discuss len(a) and len(b)
        # If len(a) != len(b), then return length of the longer string
        # If len(a) == len(b), then return -1 if a==b, or len(a) if a!=b
        pass
