class Solution:
    def catchMaximumAmountofPeople(team: list[int], dist: int) -> int:
        # Greedy algorithm: Starting from lett to right, try to allocate 0's to 1's as much as possible
        pass
