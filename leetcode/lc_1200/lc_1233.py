class Solution:
    def removeSubfolders(self, folder: list[str]) -> list[str]:
        # Sort folder list alphabetically
        # Go though the sorted list, if root_folders is empty or last item
        # in root_folders (append "/") is a prefix of the current item
        # then it's a sub folder remove it, otherwise it's a root, append
        # it to root_folders
        pass
