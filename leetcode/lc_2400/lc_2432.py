class Solution:
    def hardestWorker(self, n: int, logs: list[list[int]]) -> int:
        # Go through the list of tasks, while recording the longest
        # task so far, only update longest_task id if a task takes
        # less
        pass
