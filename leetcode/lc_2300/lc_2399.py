class Solution:
    def checkDistances(self, s: str, distance: list[int]) -> bool:
        return self.check_distances(s, distance)

    def check_distances(self, s: str, distance: list[int]) -> bool:
        # Loop through s, check distances, verify against distance
        pass
