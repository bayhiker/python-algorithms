class Solution:
    def lastVisitedIntegers(self, words: list[str]) -> list[int]:
        return self.last_visited_integers(words)

    def last_visited_integers(self, words: list[str]) -> list[int]:
        # Use a list to track list of numbers
        # Use a counter to track index of current consecutive prev
        pass
